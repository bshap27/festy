<h1><%= @artist.name %></h1>
<div class="add-artist">
	<h5 class='btn'>Add to My Artists</h5>
</div>
<h3>will be playing at <%= @artist.festivals.count %> upcoming <%= @artist.festivals.count == 1 ? "festival:" : "festivals!" %></h3>

<ul>
	<% @artist.festivals.each do |f| %>
		<li><%= link_to f.name, f %></li>
	<% end %>
</ul>
